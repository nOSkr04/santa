import { StyleSheet, Text, View } from "react-native";
import React, { memo } from "react";

const CheckVersionScreen = memo(() => {
    return (
      <View>
        <Text>CheckVersion</Text>
      </View>
    );
  });

  CheckVersionScreen.displayName="CheckVersionScreen";

export { CheckVersionScreen };

const styles = StyleSheet.create({});